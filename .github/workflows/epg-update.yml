name: Auto-update EPG

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

concurrency:
  group: epg-update
  cancel-in-progress: false

jobs:
  update-epg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Git identity
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: Download EPG sources
        run: |
          set +e
          echo "🔄 Starting EPG sync..."

          mkdir -p sources
          SUCCESS=0

          # -----------------------------
          # IPTV-ORG: all site XMLs
          # -----------------------------
          echo "📡 Fetching iptv-org site EPGs..."

          SITE_LIST=$(curl -fsSL \
            https://api.github.com/repos/iptv-org/epg/contents/sites \
            | jq -r '.[] | select(.name | endswith(".xml")) | .download_url')

          for url in $SITE_LIST; do
            file="sources/iptv-org-$(basename "$url")"
            if curl -fsSL "$url" -o "$file"; then
              echo "✅ $(basename "$file")"
              SUCCESS=$((SUCCESS+1))
            fi
          done

          # -----------------------------
          # EPGSHARE master feed
          # -----------------------------
          echo "📦 Fetching epgshare master feed..."

          if curl -fsSL https://epgshare01.online/epgshare01/epg_ripper_ALL_SOURCES1.xml.gz -o sources/epgshare.xml.gz; then
            gunzip -f sources/epgshare.xml.gz
            echo "✅ epgshare.xml"
            SUCCESS=$((SUCCESS+1))
          else
            echo "⚠️  Could not load epgshare feed"
          fi

          if [ "$SUCCESS" -eq 0 ]; then
            echo "🟡 No EPG sources available — exiting cleanly"
            exit 0
          fi

          echo "✅ Sources downloaded: $SUCCESS"

      - name: Merge EPG XMLs
        run: |
          echo "🔧 Merging EPG files..."

          echo '<?xml version="1.0" encoding="UTF-8"?>' > epg.xml
          echo '<tv>' >> epg.xml

          for f
